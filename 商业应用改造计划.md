# TextBookAgent 商业应用改造计划

## 1. 改造目标

TextBookAgent 当前已经具备教材编制、知识库整理、模型配置、记忆系统、图表/图片生成、Word 导出等核心能力，但整体形态仍偏向本地 Web 工具和原型系统。

商业应用改造的目标是将其升级为面向市场交付的成熟客户端产品：

- 从浏览器访问的 Web 控制台，升级为可安装、可启动、可配置的桌面客户端。
- 将用户教材项目、知识库资料、导出文件等“业务工作数据”和系统记忆、用户画像、配置、缓存等“系统数据”分区管理。
- 提升安全性、稳定性、可恢复性、可观测性和长期项目协作能力。
- 保持本地优先、低上下文消耗、低模型请求次数、工具调用成功率高的设计原则。

## 2. 客户端封装方向

### 2.1 推荐形态

建议采用“本地后端服务 + 桌面客户端壳”的架构。

前端仍可复用现有 `channel/web/web/textbook.html`、`static/js/textbook.js`、`static/css/textbook.css` 的主要能力，但不再要求用户手动打开浏览器访问 `http://localhost:9899/textbook`。

客户端启动后负责：

- 启动或连接本地 Python 后端服务。
- 打开内置 WebView 展示教材工作台。
- 管理工作区选择、系统区配置、日志查看、版本更新。
- 在必要时提供托盘、后台任务、崩溃恢复、服务重启等能力。

可选技术路线：

- **Tauri**：体积小、安全边界较好，适合商业客户端。
- **Electron**：生态成熟，前端改造成本低，但包体较大。
- **PySide6 / Qt WebEngine**：Python 体系内集成方便，但前端体验和打包复杂度需要评估。

初期建议优先评估 Tauri 或 Electron。

## 3. 工作区与系统区拆分

### 3.1 当前问题

当前默认工作空间集中在：

```text
C:/Users/<user>/textbook_workspace
```

该目录同时承载：

- 教材项目
- 知识库
- 记忆
- 用户画像
- 临时文件
- 聊天历史
- 导出文件
- 系统状态

这会导致几个问题：

- 用户难以理解哪些文件是“我的教材资料”，哪些是系统内部文件。
- 多个项目、多套客户资料、多套知识库难以隔离。
- 商业客户端中，用户通常希望主动选择项目文件夹。
- 备份、迁移、删除项目时容易误删系统记忆或配置。

### 3.2 目标结构

建议拆分为：

```text
用户选择的工作区/
  textbooks/
  knowledge/
  exports/
  assets/
  tmp/
  workspace.json

C:/Users/<user>/textbook_workspace/
  system/
    memory/
    user_profile.json
    sessions/
    logs/
    cache/
    jobs/
    config/
    model_registry.json
    app_state.json
```

其中：

- **工作区**：由用户在客户端界面选择，用于存放教材、知识库、导出物、项目资产。
- **系统区**：仍保留在 `C:/Users/<user>/textbook_workspace` 下，用于存放记忆、用户画像、会话索引、配置、缓存、任务状态等系统文件。

### 3.3 客户端工作区选择

客户端首次启动时应提供“选择工作文件夹”流程。

用户选择某个文件夹后，系统在其中初始化：

```text
workspace.json
textbooks/
knowledge/
exports/
assets/
tmp/
```

后续所有“新建教材”“上传知识库”“导出 Word”等业务数据都写入当前工作区。

客户端应支持：

- 最近工作区列表。
- 切换工作区。
- 打开工作区所在文件夹。
- 校验工作区结构。
- 工作区迁移与备份。
- 禁止把系统区误选为工作区，或给出明确提示。

### 3.4 系统区职责

系统区继续使用默认路径：

```text
C:/Users/<user>/textbook_workspace
```

但其内部应升级为明确的系统目录：

```text
C:/Users/<user>/textbook_workspace/system
```

系统区保存：

- 记忆系统：进程记忆、会话记忆、长期记忆索引。
- 用户画像：偏好、长期目标、项目习惯。
- 客户端配置：当前工作区、主题、模型配置引用。
- 模型供应商配置与密钥引用。
- 后台任务状态：教材生成、知识库整理、导出、图片生成。
- 日志、缓存、临时任务产物。

原则：

- 系统区不直接保存用户教材正文。
- 系统记忆可引用工作区项目，但应保存相对路径或工作区 ID。
- 用户可在客户端中清理系统缓存、导出用户画像、重置记忆。

## 4. 配置与密钥治理

### 4.1 当前风险

当前 `config.json` 仍承担过多职责，并且容易保存明文 API Key。

商业应用中应避免：

- API Key 明文散落在项目根目录。
- 前端配置、后端配置、模型配置多套结构不一致。
- 供应商、模型名、API Base、API Key 互相推断导致调用失败。

### 4.2 改造建议

配置拆分为三层：

1. **应用配置**
   - 当前工作区路径
   - 主题
   - 客户端窗口状态
   - 默认语言

2. **模型注册表**
   - 模型 ID
   - 显示名称
   - 供应商
   - 模型名
   - API Base
   - 能力标签：文本、工具调用、流式、图片生成、视觉、embedding
   - 最大上下文、推荐用途、价格估算字段

3. **密钥存储**
   - Windows Credential Manager / macOS Keychain / Linux Secret Service
   - 或本地加密密钥库
   - `config.json` 仅保存 key 引用，不保存明文 key

## 5. 安全改造

商业版必须默认安全，而不是依赖用户自行配置。

优先级最高的安全项：

- Web/客户端 API 默认启用认证，即使是本地服务也要校验客户端 token。
- 后端只监听 `127.0.0.1`，除非用户显式开启局域网访问。
- 移除环境变量解析中的 `eval`。
- 替换 `pickle` 用户数据存储。
- 工具调用增加权限策略。
- Shell 工具默认只允许工作区内操作。
- 高危命令、跨工作区文件读写、删除操作需要显式确认。
- 上传文件隔离扫描，限制文件类型和大小。
- 日志脱敏，禁止记录完整 API Key。

## 6. 任务系统改造

当前教材管线、知识库整理、图片生成等长任务仍有较多进程内状态。

商业版建议统一为 Job 系统：

```text
system/jobs/jobs.db
```

Job 类型：

- `textbook_pipeline`
- `chapter_write`
- `knowledge_organize`
- `word_export`
- `image_generate`
- `chart_generate`
- `memory_compact`

每个 Job 应包含：

- job_id
- workspace_id
- textbook_id
- status
- progress
- current_step
- created_at / updated_at / finished_at
- error
- retry_count
- event_log
- output_refs

客户端任务中心应支持：

- 查看任务进度。
- 暂停、恢复、取消。
- 失败后重试。
- 查看日志。
- 后端重启后恢复状态。

## 7. 知识库商业化改造

知识库目标是用更少上下文获取更精准资料。

建议升级为混合检索：

- LLM-WIKI 元数据索引。
- BM25 关键词检索。
- 专业 embedding 模型。
- 本地向量库或轻量向量索引。
- reranker 重排。
- 引用溯源。
- 图片、表格、公式、章节块独立索引。

客户端层面：

- 工作区内可创建多个知识库。
- 知识库可以绑定一个或多个教材。
- 新上传文件只整理未整理或已变更文件。
- 整理任务进入任务中心。
- 展示分块、摘要、使用场景、实体关系、图片资产。
- 支持“重建索引”“增量整理”“清理失效文件”。

## 8. 教材生成质量改造

商业版教材生成不应只是“生成文字”，而要形成可审计的工程链路。

建议每章生成时固定产出：

- 当前章草稿。
- 引用的知识库证据列表。
- 使用的 Web 证据列表。
- 图表需求列表。
- 图片需求列表。
- 审查报告。
- 修订记录。
- 章节摘要。
- 与前序章节的连续性说明。

图表/图片优先级：

1. 图表类需求：优先用代码和真实数据生成。
2. 知识库已有图片：优先复用。
3. 合规网络图片：需有来源和许可。
4. 图片生成模型：用于示意图、概念图、场景图。
5. 插入正文并写入资产索引。

## 9. 前端/客户端体验改造

### 9.1 当前前端问题

当前前端是单体 JS，状态散落，长期维护成本高。

主要问题：

- `textbook.js` 过长。
- 聊天、管线、知识库、设置页逻辑耦合。
- 会话恢复、任务恢复、工作区切换缺少统一状态管理。
- 部分中文文案存在编码损坏。
- Web 页面风格仍偏管理后台，不像成熟客户端产品。

### 9.2 改造建议

客户端 UI 应拆为：

- 工作区选择页
- 教材工作台
- 知识库管理
- AI 对话
- 任务中心
- 系统设置
- 模型配置
- 记忆与用户画像管理
- 日志与诊断

前端状态建议模块化：

- `workspaceStore`
- `textbookStore`
- `chatStore`
- `jobStore`
- `knowledgeStore`
- `settingsStore`
- `modelStore`

客户端顶部应明确显示：

- 当前工作区名称
- 当前教材
- 当前模型
- 当前任务状态
- 后端连接状态

## 10. 记忆系统改造

当前已经具备进程级记忆、用户画像、统一查询 API 的基础。

商业版应继续完善：

- 系统区持久保存记忆。
- 工作区项目产生的记忆应带 workspace_id。
- 用户画像区分“全局用户画像”和“项目偏好”。
- 支持用户查看、编辑、删除画像条目。
- 支持定期压缩和手动压缩。
- 支持“本项目记忆”和“全局记忆”检索。
- 记忆注入模型前必须有预算控制。

## 11. 可观测性与质量评测

商业产品需要知道系统为什么慢、为什么失败、为什么生成质量差。

建议增加：

- 模型调用日志：耗时、token、费用、错误码。
- 工具调用日志：命令、耗时、退出码、输出摘要。
- 检索日志：query、命中 chunk、分数、是否被采用。
- 图片生成日志：模型、prompt、输出路径、失败原因。
- 章节生成质量评分。
- 用户反馈入口。

测试体系建议增加：

- Playwright 端到端测试。
- 客户端启动测试。
- 工作区切换测试。
- 知识库整理基准测试。
- Word 导出结构测试。
- 模型调用 mock 合约测试。
- 安全边界测试。

## 12. 实施路线

### 阶段一：商业安全底座

- 修复配置安全问题。
- 移除 `eval`。
- 替换 `pickle`。
- 默认启用本地认证 token。
- 后端只监听 `127.0.0.1`。
- 增加工作区/系统区路径抽象。
- 新增客户端工作区选择设计。

### 阶段二：客户端封装

- 选择 Tauri/Electron/PySide6 技术路线。
- 客户端启动本地后端。
- 内置 WebView。
- 增加工作区选择页。
- 最近工作区列表。
- 后端健康检查与自动重启。
- 客户端日志查看。

### 阶段三：任务系统

- 引入持久 Job 表。
- 教材管线迁移为 Job。
- 知识库整理迁移为 Job。
- 图片生成/Word 导出迁移为 Job。
- 前端新增任务中心。

### 阶段四：前端产品化

- 拆分 `textbook.js`。
- 修复乱码文案。
- 统一设计系统。
- 优化 AI 对话、Todo 流式输出、知识图谱、模型配置。
- 增加工作区、系统区、任务中心、日志诊断页面。

### 阶段五：知识库与内容质量

- 接入 embedding。
- 增加向量库/向量索引。
- 混合检索与 reranker。
- 引用溯源。
- 图表真实数据链。
- 章节质量评测与审查报告。

## 13. 关键设计结论

1. 应该在客户端界面增加“选择工作文件夹”功能。
2. 用户选择的文件夹应作为业务工作区，用于新建教材、新建知识库、导出文件和项目资产。
3. 系统区建议继续保留在 `C:/Users/<user>/textbook_workspace` 下，但应整理为 `system/` 子目录。
4. 记忆、用户画像、会话索引、模型注册表、任务状态、日志、缓存属于系统区。
5. 教材正文、知识库原始文件、导出文件、项目资产属于工作区。
6. 客户端封装不应只是“套壳网页”，还应承担工作区管理、后端生命周期管理、任务中心和诊断能力。
7. 商业应用改造应先做安全和数据边界，再做 UI 美化和功能扩展。
