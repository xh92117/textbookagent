# Optional integrations. Install only the parts you actually use, for example:
#   python -m pip install -r requirements-optional.txt
#
# The default requirements.txt intentionally excludes these packages to avoid
# unnecessary resolver/build failures on fresh Python 3.13/3.14 environments.

# Browser tool
playwright>=1.49.0

# WeChat/WeCom/Feishu/DingTalk integrations.
# Note: the current repository snapshot must also contain the corresponding
# channel/* backend modules before these channels can run.
qrcode>=7.4.2
wechatpy>=1.8.18
lark-oapi>=1.5.5
dingtalk_stream>=0.24.0
websocket-client>=1.8.0
pycryptodome>=3.20.0

# Provider SDKs used only when selecting those providers directly.
zai-sdk>=0.0.4
dashscope>=1.20.0

# Optional legacy/plugin/voice dependencies.
docx2txt>=0.8
pyttsx3>=2.90
tencentcloud-sdk-python>=3.0.0
