# 迭代通用性 Bug 模式与修复方法

更新时间：2026-05-20

本文档总结近期开发和迭代过程中暴露出的通用性 Bug 模式。它不是某个具体项目的专属缺陷清单，而是面向智能体系统、前后端协同系统、长任务系统、知识库系统和商业化客户端系统的通用工程经验。每类问题均包含问题描述、使用对策和修复方法，方便后续开发新项目时复用。

## 1. 长任务无进度反馈

### 问题描述

长时间运行的任务如果只在最终完成时返回结果，用户会误以为系统卡死。典型场景包括：

- 大模型长上下文推理。
- 多工具连续调用。
- 文件解析、知识库整理、章节生成、图片生成。
- 后端仍在工作，但前端没有任何可见状态。

这类问题本质上不是单纯的性能问题，而是“任务生命周期不可观察”。

### 使用对策

- 用户侧应避免重复点击提交，先查看任务状态、日志或进程记录。
- 长任务应允许用户看到“正在做什么”，而不是只看到空白等待。
- 对超过数秒的任务，应默认展示阶段状态。

### 修复方法

- 为任务设计标准生命周期：
  - `queued`
  - `started`
  - `running`
  - `tool_running`
  - `waiting_model`
  - `saving`
  - `completed`
  - `failed`
- 后端通过事件流、WebSocket、SSE 或轮询接口持续上报状态。
- 前端显示固定状态条、进度列表或任务面板。
- 每个阶段写入持久化状态文件或任务数据库，刷新页面后可恢复。

## 2. 流式输出打断用户阅读

### 问题描述

流式输出系统常见问题是：只要有新内容到达，前端就强制滚动到底部，导致用户无法查看前面内容。

这在 AI 对话、日志面板、任务输出面板中都很常见。

### 使用对策

- 用户主动向上滚动后，系统不应强制拉回底部。
- 只有用户处于底部附近时，才自动追踪最新输出。
- 可提供“回到底部”按钮。

### 修复方法

- 前端增加 `isNearBottom()` 判断。
- 流式更新前记录用户滚动位置。
- 仅当用户接近底部时执行自动滚动。
- 将“当前任务状态”与“历史输出正文”分离，状态条固定，正文可自由滚动。

## 3. 前后端状态不一致

### 问题描述

前端展示的状态和后端真实状态不一致，是复杂应用最常见的 Bug 之一。表现包括：

- 前端显示任务仍在运行，后端已失败。
- 后端已完成，前端没有刷新。
- 页面切换后当前对话或任务状态丢失。
- 刷新页面后无法恢复之前的运行状态。

### 使用对策

- 不要把关键状态只存在前端内存。
- 用户刷新页面后，应能从后端重新加载任务状态。
- 前端显示异常时，应优先查询后端状态接口或状态文件。

### 修复方法

- 后端作为状态真源，前端只做渲染。
- 每个任务分配稳定 ID。
- 将任务状态、阶段、错误、结果路径持久化。
- 前端页面初始化时，通过任务 ID 重新拉取状态。
- 避免多个状态源同时写同一份业务状态。

## 4. 配置来源混乱

### 问题描述

配置系统如果缺乏统一来源，会出现：

- 前端保存了配置，但后端没有生效。
- 配置文件 A 被改了，运行时读取配置文件 B。
- 删除模型或 API 后，界面仍显示旧数据。
- 默认配置、用户配置、运行时配置互相覆盖。

### 使用对策

- 明确唯一配置真源。
- 修改配置后，通过接口读取一次配置确认是否保存成功。
- 用户配置和模板配置必须分离。

### 修复方法

- 设计配置层级：
  - 默认配置模板
  - 用户配置
  - 环境变量覆盖
  - 运行时临时覆盖
- 前端只调用配置 API，不直接假设配置结构。
- 保存后由后端返回最终生效配置。
- 对配置做 schema 校验和版本迁移。
- 配置中保存“模型实例”，不要只保存“供应商名称”。

## 5. 供应商与模型强绑定

### 问题描述

很多 AI 平台支持兼容模式，例如一个供应商的接口可以调用其他供应商风格的模型。如果前端按供应商硬编码模型列表，会导致：

- 兼容模型无法配置。
- 模型切换失败。
- 用户明明有 API Key，前端却不允许选择对应模型。

### 使用对策

- 用户应按“模型实例”配置，而不是按“供应商默认模型”配置。
- 每个模型实例应包含名称、Base URL、API Key、能力标签。

### 修复方法

- 把模型配置抽象为：
  - `display_name`
  - `provider`
  - `model_name`
  - `base_url`
  - `api_key`
  - `capabilities`
- UI 中供应商只用于预填 URL，不限制模型名称。
- 审核模型、图片模型、知识库模型从已配置模型实例中选择。

## 6. 上下文无限增长

### 问题描述

对话历史、工具返回、知识库片段、文件内容不断进入上下文，会导致：

- 请求超出模型上下文。
- 响应变慢。
- 成本升高。
- 模型注意力分散，回答跑偏。
- 工具结果过长造成接口报错。

### 使用对策

- 不要把历史全文、知识库全文、所有文件全文塞给模型。
- 使用摘要、索引和按需检索。
- 工具输出必须限制长度。

### 修复方法

- 建立上下文预算：
  - 系统提示预算
  - 用户当前任务预算
  - 历史摘要预算
  - 工具结果预算
  - 知识片段预算
- 长期信息只加载索引，必要时再调用检索工具。
- 对历史消息做周期性压缩。
- 对工具结果做 head/tail 截断。
- 对大型任务使用“全局大纲 + 当前局部详情 + 必要前文摘要”。

## 7. 记忆系统过度加载或不落盘

### 问题描述

智能体记忆系统常见两类极端：

- 每次请求加载过多记忆，导致上下文膨胀。
- 只在内存中记忆，进程结束后全部丢失。

还有一种隐蔽问题：记忆文件存在，但没有接入真实运行链路。

### 使用对策

- 启动时只加载用户画像和记忆索引。
- 长期记忆应通过工具按需调用。
- 错误记忆、用户纠正、任务状态必须自动落盘。

### 修复方法

- 将记忆分层：
  - 会话/进程记忆
  - 用户画像
  - 错误记忆
  - 长期记忆
  - 项目/任务状态记忆
- 记忆调用规则：
  - 启动时加载轻量索引。
  - 需要详情时一次性检索。
  - 错误发生时写入错误记忆。
  - 用户纠正时写入纠正记忆。
- 每类记忆都要有元数据、摘要和路径，不直接把全文塞进提示词。

## 8. 错误没有形成可复用经验

### 问题描述

工具调用失败、路径错误、权限错误、用户纠正、模型空回复等问题如果只写日志，不进入智能体记忆，就会反复出现。

### 使用对策

- 用户纠正不是普通聊天内容，应视为高价值反馈。
- 工具失败后，下一轮应优先参考近期错误记录。

### 修复方法

- 捕捉以下事件：
  - 工具返回错误。
  - 工具抛异常。
  - Agent 总体报错。
  - 用户明确纠正。
  - 模型连续空回复。
- 写入结构化错误记忆：
  - 错误类型
  - 工具名
  - 输入参数摘要
  - 错误详情
  - 修复建议
  - 时间
  - 相关任务 ID
- 工具失败后，给模型追加最近错误索引，而不是追加全部错误全文。

## 9. 文件路径基准不清晰

### 问题描述

路径问题在桌面端、Web 端、后端任务系统中非常常见。表现包括：

- 相对路径解析到错误目录。
- 系统区和工作区混用。
- 用户上传文件、知识库文件、系统记忆文件放在同一目录。
- 清理工作区时误删源码或系统文件。

### 使用对策

- 所有错误提示必须显示 resolved path。
- 用户数据和程序源码必须隔离。
- 清理前先列出目标路径，确认位于工作区。

### 修复方法

- 拆分路径域：
  - 应用安装目录
  - 系统区
  - 用户工作区
  - 缓存区
  - 临时区
- 所有文件工具都通过统一路径解析器。
- 不允许业务代码手写路径拼接规则。
- 清理任务增加路径白名单和 dry-run。

## 10. 数据清理误伤

### 问题描述

测试前清理工作区时，如果递归删除命令范围过宽，可能误删源码、Git 元数据、依赖文件或用户数据。

这是高危工程事故，不是普通 Bug。

### 使用对策

- 不在源码根目录执行宽泛递归删除。
- 清理脚本必须先 dry-run。
- 只清理用户工作区，不清理程序目录。

### 修复方法

- 清理逻辑必须包含：
  - 目标路径归一化。
  - 路径必须位于允许目录内。
  - 删除前输出待删除列表。
  - 禁止删除 `.git`、源码目录、配置模板、依赖文件。
  - 日志记录每次删除操作。
- Windows 下避免跨 shell 拼接删除命令。

## 11. 知识库索引过重

### 问题描述

知识库如果把大量正文、分块、图谱、向量信息都堆到根索引，会导致：

- 索引文件巨大。
- 检索慢。
- 上下文难以控制。
- 增量整理困难。
- 前端加载卡顿。

### 使用对策

- 根索引只存文件级入口。
- 文件详情、分块、图谱、图片等信息放在子索引。
- 检索时先找文件，再找章节，再找分块。

### 修复方法

- 采用分层索引：
  - 根索引：源文件列表和状态。
  - 文件索引：章节、摘要、关键词。
  - 分块索引：chunk 摘要、适用场景、引用范围。
  - 资产索引：图片、表格、公式。
  - 图谱索引：实体和关系。
- 整理任务只处理未整理文件。
- 每个分块保留“什么时候使用这个分块”的元数据。

## 12. 文档解析损失结构

### 问题描述

PDF、Word、论文等文件解析时，如果只提取纯文本，会丢失大量结构：

- 标题层级
- 表格
- 公式
- 图片
- 图注
- 页码
- 引用关系

这会导致知识库质量差，后续生成内容空泛。

### 使用对策

- 论文和教材类文档应优先按章节结构切分。
- 表格、公式、图片应作为结构化资产保存。

### 修复方法

- 分块策略：
  - 先按章节切。
  - 章节过大再按 token 切。
- 表格保存为 Markdown。
- 公式保存为 LaTeX。
- 图片抽取到独立目录。
- 图注和图片路径写入索引。
- 每个 chunk 生成摘要、关键词、适用场景和引用位置。

## 13. 知识图谱一次性加载过多

### 问题描述

知识图谱节点和边过多时，前端一次性渲染会卡顿甚至崩溃。

### 使用对策

- 默认不要展示全图。
- 先展示核心节点或搜索结果。
- 用户点击后再展开邻居。

### 修复方法

- 图谱接口支持分页、搜索、按类型过滤、按深度展开。
- 前端使用虚拟化或 Canvas/WebGL 渲染。
- 节点布局缓存，避免每次切换都重新计算全图。
- 限制默认节点数和边数。

## 14. 图片生成和图表生成混用

### 问题描述

把“数据图表”和“插画图片”都交给图片生成模型，会导致：

- 图表数字不可信。
- 文字模糊。
- 坐标轴错误。
- 图片看不清。
- 教材内容不专业。

### 使用对策

- 图表优先用代码和真实数据生成。
- 图片生成模型只用于概念插画、场景图、示意图。

### 修复方法

- 插图决策链：
  - 是否为图表：是则代码生成。
  - 知识库是否有图：有则复用。
  - 网络是否有合适图：有则引用。
  - 否则调用图片生成模型。
- 对每张图记录来源、生成方式、提示词、数据来源和插入位置。

## 15. 网络搜索结果没有沉淀

### 问题描述

智能体每次都临时搜索网络，但不保存有价值资料，会造成：

- 重复搜索。
- 成本升高。
- 结果不可复现。
- 后续章节无法复用前面资料。

### 使用对策

- 对搜索结果进行价值判定。
- 有价值内容保存为知识库源文件。

### 修复方法

- 搜索后增加资料捕获流程：
  - 判断是否与当前任务相关。
  - 提取标题、URL、摘要、关键事实、使用场景。
  - 保存为 Markdown。
  - 加入知识库源文件目录。
  - 后续通过知识库检索优先使用。

## 16. 多工具调用缺少失败降级

### 问题描述

工具链如果没有降级策略，一个工具失败可能导致整个任务失败。典型例子：

- Google 搜索失败后没有切换百度。
- 图片生成失败后没有查知识库图片。
- 文件读取失败后没有列目录确认路径。

### 使用对策

- 用户任务不要依赖单一工具。
- 工具失败后应自动尝试替代路径。

### 修复方法

- 每类能力设计主路径和备用路径。
- 工具错误返回中包含下一步建议。
- Agent 提示词中明确失败后降级策略。
- 错误记忆记录“某工具在什么条件下失败”。

## 17. 前端页面缺少大数据保护

### 问题描述

前端一次性渲染大量消息、图谱节点、日志、工具结果，会导致页面卡顿。

### 使用对策

- 不展示全部历史。
- 默认折叠大输出。
- 用户需要时再展开。

### 修复方法

- 消息列表虚拟滚动。
- 工具结果折叠展示。
- 大图谱分页或按需展开。
- 长 Markdown 分段渲染。
- 图片和附件懒加载。

## 18. UI 状态与业务语义混杂

### 问题描述

很多前端 Bug 来自把 UI 状态当业务状态使用。例如：

- 当前选中的 tab 被当成任务状态。
- 当前滚动位置影响消息是否保存。
- 前端临时数组被当成聊天历史真源。

### 使用对策

- UI 状态只负责展示。
- 业务状态必须由后端或持久化 store 管理。

### 修复方法

- 区分：
  - UI state
  - session state
  - task state
  - persistent business state
- 业务状态变更必须走统一 action/API。
- 前端刷新后可从后端完整恢复。

## 19. 测试只测函数，不测链路

### 问题描述

单个函数测试通过，不代表功能真的可用。常见情况：

- 记录器能写文件，但没有接入 Agent。
- 配置保存函数正常，但前端没有调用正确接口。
- 工具可单独执行，但流式事件没有显示。

### 使用对策

- 关键能力必须做端到端测试。
- 每个“可见功能”至少覆盖一次真实入口。

### 修复方法

- 测试分层：
  - 单元测试
  - 集成测试
  - API 测试
  - 前端交互测试
  - 端到端任务测试
- 对长任务、记忆、工具调用、配置保存、文件路径做专门回归测试。

## 20. 商业化客户端路径和权限问题

### 问题描述

Web 项目封装为客户端后，路径、权限和依赖问题会集中出现：

- 用户不知道数据保存在哪里。
- 安装目录不可写。
- 系统区和工作区混乱。
- 多工作区切换后数据串扰。
- 依赖安装失败导致功能不可用。

### 使用对策

- 客户端首次启动应引导用户选择工作区。
- 系统区和工作区必须清晰展示。
- 重要数据提供导出和备份入口。

### 修复方法

- 路径设计：
  - 系统区：配置、记忆、用户画像、缓存、日志。
  - 工作区：教材、知识库、导出文件、用户上传文件。
- 客户端设置页提供工作区选择和迁移。
- 依赖检测与修复入口可视化。
- 所有路径变更写入配置并立即生效。

## 通用回归检查清单

每次大迭代后建议按以下清单回归：

- 长任务是否有持续状态反馈。
- 页面刷新后任务和对话能否恢复。
- 用户滚动查看历史时是否被强制跳底。
- 配置保存后，重启是否仍然生效。
- 模型切换是否按模型实例而非供应商硬编码。
- 工具失败是否写入错误记忆。
- 用户纠正是否进入纠正记忆。
- 长期记忆是否按需检索，而不是每轮加载全文。
- 知识库整理是否只处理新增文件。
- 根索引是否足够轻量。
- 大图谱、大日志、大消息是否有懒加载或分页。
- 文件路径是否明确区分系统区和工作区。
- 清理任务是否具备 dry-run 和路径白名单。
- 图片和图表是否走不同生成策略。
- 网络搜索结果是否能沉淀为知识。
- 端到端测试是否覆盖真实用户入口。
